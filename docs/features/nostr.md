nostr protocol allows for decentralized relaying of user messages across the internet.

It's integration should "just work" and allow the user more freedom and censorship-resistant ease-of-use - magically.

Only advanced users may be able to notice that data sync and social sharing is accomplished via a background nostr protocol layer.